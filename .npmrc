# Todas las dependencias de este proyecto son publicas. Fijar el registry oficial
# evita errores E401 en maquinas con ~/.npmrc corporativo (registry privado con
# credenciales vencidas). Este archivo solo aplica dentro de esta carpeta.
registry=https://registry.npmjs.org/
