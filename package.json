{
  "name": "claude-multi-instance",
  "version": "1.1.0",
  "private": true,
  "description": "Dashboard local para correr N sesiones de Claude Code en paralelo, cada una en un slot fijo (clon completo del repo), con persistencia via tmux",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.11"
  }
}
